# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @microservices/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t microservices-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system microservices-auth-service && \
          adduser --system -G microservices-auth-service microservices-auth-service

COPY apps/auth-service/dist microservices-auth-service/
COPY apps/auth-service/package.json microservices-auth-service/
RUN chown -R microservices-auth-service:microservices-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix microservices-auth-service --omit=dev -f install

CMD [ "node", "microservices-auth-service" ]
